version: '3'

services:
  website:
    build: .
    command: >
      gunicorn -b 0.0.0.0:8000
        --access-logfile -
        --reload
        "playmove.application:create_app()"
    environment:
      PYTHONUNBUFFERED: 'true'
    volumes:
      - '.:/playmove-app'
    ports:
      - '8000:8000'
