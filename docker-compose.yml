version: '3'

services:
  website:
    build: .
    commad: >
      gunicorn -b 0.0.0.0:8000
        --access-logfile -
        --reload
        "play-move.app:create_app()"
    environment:
      PYTHONUNBUFFERED: 'true'
    volumes:
      - '.:/play-move-app'
    ports:
      - '8000:8000'
